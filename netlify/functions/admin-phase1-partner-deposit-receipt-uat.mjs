import { getAdminSession } from './_partner-core.mjs';
import { buildPartnerDepositReceiptUatReadiness, executePartnerDepositReceiptUat, getPartnerDepositReceiptUatState, reconcilePartnerDepositReceiptUat, requestPartnerDepositReceiptUat } from './_accurate-partner-deposit-receipt-uat-core.mjs';

const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const rup=v=>`Rp${Math.round(Number(v)||0).toLocaleString('id-ID')}`;
function sameOrigin(request){const u=new URL(request.url),o=request.headers.get('origin'),r=request.headers.get('referer');if(o)return o===u.origin;if(r){try{return new URL(r).origin===u.origin}catch{return false}}return false;}
function form(action,label,reference,booking){return `<form method="post" class="action"><input type="hidden" name="action" value="${esc(action)}"><input type="hidden" name="reference" value="${esc(reference)}">${booking?`<input type="hidden" name="booking" value="${esc(booking)}">`:''}<label><input type="checkbox" name="confirm" value="YES" required> Saya memahami aksi ini hanya database TEST dan tidak memutasi wallet/production.</label><button>${esc(label)}</button></form>`;}

export default async request=>{
 const session=getAdminSession(request);if(!session)return Response.redirect(new URL('/libra-admin-login.html',request.url),302);const role=String(session.role||'').toUpperCase();if(!['SUPERADMIN','FINANCE'].includes(role))return new Response('Akses ditolak.',{status:403});if(!['GET','POST'].includes(request.method))return new Response('Method not allowed',{status:405});
 const url=new URL(request.url);let reference=String(url.searchParams.get('reference')||'').trim(),booking=String(url.searchParams.get('booking')||'').trim(),message='',actionError='';
 if(request.method==='POST')try{if(!sameOrigin(request))throw new Error('Origin form tidak valid.');const f=await request.formData();reference=String(f.get('reference')||reference).trim();booking=String(f.get('booking')||booking).trim();if(!reference)throw new Error('Reference top-up wajib.');if(String(f.get('confirm')||'')!=='YES')throw new Error('Konfirmasi wajib dicentang.');const action=String(f.get('action')||'').toUpperCase();if(action==='REQUEST'){const x=await requestPartnerDepositReceiptUat({referenceId:reference,session,request});message=x.idempotent?`Request ${x.request.requestId} sudah ada.`:`Request ${x.request.requestId} dibuat.`;}else if(action==='EXECUTE'){const x=await executePartnerDepositReceiptUat({referenceId:reference,session,request});message=x.idempotent?'Penerimaan DP UAT sudah VERIFIED.':`Penerimaan DP UAT ${x.request.execution?.accurateNumber||'-'} berhasil dan read-back VERIFIED.`;}else if(action==='RECONCILE'){const x=await reconcilePartnerDepositReceiptUat({referenceId:reference,session,request});message=x.idempotent?'Penerimaan DP UAT sudah EXECUTED.':`Reconcile selesai: ${x.request.execution?.accurateNumber||'-'} VERIFIED tanpa POST ulang.`;}else throw new Error('Aksi tidak dikenali.');}catch(e){actionError=e?.message||'Aksi Receipt UAT gagal.';}
 let readiness=null,state=null,readError='';if(reference){try{state=await getPartnerDepositReceiptUatState(reference);}catch{}try{readiness=await buildPartnerDepositReceiptUatReadiness(reference);}catch(e){readError=e?.message||'Readiness gagal.';}}
 const status=state?.status||'BELUM ADA REQUEST',klass=status==='EXECUTED'?'good':status==='RECONCILE_REQUIRED'?'bad':'hold',back=booking?`/admin-phase1-partner-deposit-dp-uat?reference=${encodeURIComponent(reference)}&booking=${encodeURIComponent(booking)}`:`/admin-phase1-partner-deposit-dp-uat?reference=${encodeURIComponent(reference)}`;
 const forms=reference?`${!state||state.status==='POST_FAILED'?form('REQUEST','Buat Request Receipt UAT',reference,booking):''}${state?.status==='PENDING'?form('EXECUTE','Execute Penerimaan DP TEST',reference,booking):''}${state?.status==='RECONCILE_REQUIRED'?form('RECONCILE','Reconcile Tanpa POST Ulang',reference,booking):''}`:'';
 const html=`<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Partner Deposit Receipt UAT | Libra</title><style>*{box-sizing:border-box}body{margin:0;background:#f3f6f9;color:#10243d;font-family:Inter,system-ui,-apple-system,sans-serif}.top{background:#061d36;color:#fff;padding:16px}.topin,.wrap{max-width:1080px;margin:auto}.topin{display:flex;justify-content:space-between;gap:12px}.top a{color:#fff}.wrap{padding:22px 14px 60px}.hero,.card{background:#fff;border:1px solid #dbe5ee;border-radius:18px;padding:17px;margin-bottom:14px}.hero{background:linear-gradient(135deg,#072746,#0b5c9c);color:#fff}.hero h1{margin:0 0 6px}.hero p{margin:0;color:#d8e8f6}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.grid div{background:#f7fafc;border-radius:11px;padding:11px}.grid small,.grid b{display:block}.grid small{color:#718397;font-size:10px}.good,.bad,.hold{display:inline-block;border-radius:999px;padding:7px 10px;font-size:10px;font-weight:900}.good{background:#e3f6e8;color:#176c37}.bad,.err,.danger{background:#ffe9e6;color:#9f3028}.hold{background:#edf2f6;color:#53697c}.info,.err,.danger,.msg{padding:12px;border-radius:11px;line-height:1.5;font-size:12px}.info{background:#eaf5ff;color:#0b588e}.msg{background:#e3f6e8;color:#176c37}.mono{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;background:#f7fafc;padding:11px;border-radius:11px;white-space:pre-wrap;font-size:11px}.lookup{display:flex;gap:8px;flex-wrap:wrap}.lookup input{flex:1;min-width:240px;border:1px solid #cbd8e3;border-radius:10px;padding:11px}.btn,.action button{border:0;border-radius:10px;padding:11px 14px;font-weight:800;cursor:pointer;background:#0b5c9c;color:#fff}.actions{display:flex;gap:10px;flex-wrap:wrap}.action{border:1px solid #dbe5ee;border-radius:12px;padding:12px;background:#f9fbfd}.action label{display:block;font-size:11px;margin:6px 0}@media(max-width:700px){.grid{grid-template-columns:1fr 1fr}.topin{display:block}}</style></head><body><header class="top"><div class="topin"><strong>LIBRA FINANCE • DP RECEIPT UAT</strong><a href="${esc(back)}">← Kembali ke DP UAT</a></div></header><main class="wrap"><section class="hero"><h1>Top-up → DP → Penerimaan DP TEST</h1><p>Tahap kedua proof Partner Deposit. Production dan wallet tetap terkunci.</p></section>${message?`<div class="msg">${esc(message)}</div>`:''}${actionError?`<div class="err">${esc(actionError)}</div>`:''}<section class="card"><h2>Cari Reference Top-up</h2><form method="get" class="lookup"><input name="reference" value="${esc(reference)}" placeholder="LBRTP-..." required>${booking?`<input type="hidden" name="booking" value="${esc(booking)}">`:''}<button class="btn">Cek</button></form></section>${reference?`<section class="card"><div class="grid"><div><small>Reference</small><b>${esc(reference)}</b></div><div><small>Status</small><b><span class="${klass}">${esc(status)}</span></b></div><div><small>Database TEST</small><b>${esc(readiness?.databaseName||state?.databaseName||'-')}</b></div><div><small>Partner</small><b>${esc(readiness?.partnerId||state?.partnerId||'-')}</b></div><div><small>Nominal</small><b>${rup(readiness?.amount??state?.amount)}</b></div><div><small>DP Accurate</small><b>${esc(readiness?.dpAccurateNumber||state?.dpAccurateNumber||'-')}</b></div><div><small>Customer</small><b>${esc(readiness?.customerNo||state?.customerNo||'-')}</b></div><div><small>Bank TEST</small><b>${esc(readiness?.bankNo||state?.bankNo||'-')}</b></div></div></section>${readError?`<div class="err"><b>BLOCKED:</b> ${esc(readError)}</div>`:''}${readiness?`<section class="card"><h2>Payload TEST</h2><div class="info">Penerimaan diarahkan ke DP <b>${esc(readiness.dpAccurateNumber)}</b> dengan nominal exact ${rup(readiness.amount)}. Tidak ada mutasi wallet.</div><div class="mono">${esc(JSON.stringify(readiness.payload,null,2))}</div></section>`:''}${state?.execution?`<section class="card"><h2>Execution / Reconciliation</h2><div class="mono">${esc(JSON.stringify(state.execution,null,2))}</div></section>`:''}<section class="card"><div class="actions">${forms}</div></section>`:''}<section class="card"><div class="danger"><b>Production masih BLOCKED.</b> Setelah Receipt UAT VERIFIED, proof berikutnya adalah memakai DP tersebut sebagai <code>detailDownPayment</code> pada Native Sales Invoice TEST dan memverifikasi sisa piutang/read-back.</div></section></main></body></html>`;
 return new Response(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'"}});
};
export const config={path:'/admin-phase1-partner-deposit-receipt-uat'};
