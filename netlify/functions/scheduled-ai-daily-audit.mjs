import { generateControlTowerReport } from './_ai-control-tower-core.mjs';
import { createOperationalNotification } from './_notification-core.mjs';
import { markSystemHeartbeat } from './_system-heartbeat-core.mjs';

export default async ()=>{try{const report=await generateControlTowerReport({periodHours:24,kind:'DAILY',source:'SCHEDULED_DAILY_07_WIT'});await createOperationalNotification({type:'AI_DAILY_AUDIT_RESUME',severity:report.risk==='RED'?'CRITICAL':report.risk==='YELLOW'?'WARNING':'SUCCESS',title:`Daily Audit Resume • ${report.risk} ${report.score}/100`,message:`Audit Chain ${report.snapshot.audit.chainValid?'VALID':'INVALID'} • Accurate POSTED ${report.snapshot.accurate.counts.POSTED||0} • Reconcile ${report.snapshot.accurate.counts.RECONCILE_REQUIRED||0} • SLA RED ${report.snapshot.sla.red||0}. Buka AI Control Tower untuk resume lengkap.`,notifyPartner:false,notifyAdmin:true,adminLink:'/admin-ai-control-tower',dedupeKey:`ai-daily:${report.period.endAt.slice(0,10)}`,metadata:{reportId:report.reportId,risk:report.risk,score:report.score,aiUsed:report.aiUsed}}).catch(()=>{});console.log(JSON.stringify({event:'LIBRA_AI_AUDIT_RESUME',kind:'DAILY',reportId:report.reportId,risk:report.risk,score:report.score,aiUsed:report.aiUsed}));}catch(error){await markSystemHeartbeat('AI_CONTROL_TOWER',{status:'ERROR',message:`Daily audit gagal: ${String(error?.message||error).slice(0,430)}`}).catch(()=>{});throw error;}};
// Netlify cron UTC: 22:00 UTC = 07:00 WIT (UTC+9) hari berikutnya.
export const config={schedule:'0 22 * * *'};
