import { getStore } from '@netlify/blobs';
import { accurateConfigStatus, resolveAccurateConnection, validateAccurateAccountMap, validateAccurateBranch } from './_accurate-core.mjs';
import { assertAdminPermission } from './_admin-rbac-core.mjs';
import { getAdminSession } from './_partner-core.mjs';

const STORE='libra-accurate-sync';
const store=()=>getStore(STORE);
const clean=(v,n=300)=>String(v??'').trim().slice(0,n);
const esc=v=>String(v??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
const dbName=db=>clean(db?.alias||db?.name||db?.databaseName||db?.companyName||'',160);
const isTest=name=>/(test|tes|uat|sandbox)/i.test(String(name||''));
const expectedProductionName=()=>clean(process.env.ACCURATE_PRODUCTION_DATABASE_NAME,160);

async function strongUatPass(branchName){const {blobs}=await store().list({prefix:'job/'}),rows=[];for(const blob of blobs){const entry=await store().getWithMetadata(blob.key,{type:'json',consistency:'strong'}),row=entry?.data;if(row?.liveUat)rows.push(row);}const target=String(branchName||'').trim().toLowerCase();const passed=rows.filter(r=>r.status==='POSTED'&&String(r.journalNumber||'').startsWith('LBR-UAT-ACC-')&&Number(r.journalDraft?.totalDebit)===10000&&Number(r.journalDraft?.totalCredit)===10000&&r.journalDraft?.balanced===true&&String(r.journalDraft?.branchName||r.uatBranchName||'').trim().toLowerCase()===target).sort((a,b)=>String(b.postedAt||'').localeCompare(String(a.postedAt||'')))[0]||null;return {passed:Boolean(passed),job:passed,totalUat:rows.length};}

export default async request=>{
 const session=getAdminSession(request);if(!session)return Response.redirect(new URL('/libra-admin-login.html',request.url),302);try{assertAdminPermission(session,'finance.reconcile');}catch{return new Response('Forbidden',{status:403});}if(request.method!=='GET')return new Response('Method not allowed',{status:405});
 const config=accurateConfigStatus(),expected=expectedProductionName();let connection=null,currentName='',connectionError='',validation={ok:false,mapped:[]},branch={ok:false,target:config.branchName||'JLX Cargo',found:null};if(config.configured){try{connection=await resolveAccurateConnection();currentName=dbName(connection.database);validation=await validateAccurateAccountMap();branch=await validateAccurateBranch();}catch(e){connectionError=clean(e?.message||e,500);}}
 const uat=await strongUatPass(branch.target),currentIsTest=isTest(currentName),expectedSet=Boolean(expected),identityMatch=expectedSet&&Boolean(currentName)&&currentName.toLowerCase()===expected.toLowerCase(),productionDbVerified=Boolean(connection&&!currentIsTest&&identityMatch);
 const key1Locked=!config.postingEnabled,key2Locked=!config.productionArmed,baseReady=Boolean(uat.passed&&config.configured&&config.mappingReady&&validation.ok&&branch.ok&&productionDbVerified),readyToArm=Boolean(baseReady&&key1Locked&&key2Locked),liveArmed=Boolean(baseReady&&config.postingEnabled&&config.productionArmed),partialArm=Boolean(config.postingEnabled!==config.productionArmed);
 const badge=(ok,yes='PASS',no='BELUM')=>`<b class="${ok?'ok':'bad'}">${ok?yes:no}</b>`,mappingRows=(validation.mapped||[]).map(x=>`<tr><td>${esc(x.role)}</td><td>${esc(x.no)}</td><td>${x.found?'<b class="ok">FOUND</b>':'<b class="bad">MISSING</b>'}</td><td>${esc(x.name||'-')}</td></tr>`).join('');
 let title='Production masih terkunci',intro='Production tidak akan dibuka sebelum database actual, COA actual, Cabang JLX Cargo, identity lock, dan UAT cabang seluruhnya lolos.';if(readyToArm){title='READY TO ARM — belum go-live';intro='Semua precondition production sudah PASS dan kedua kunci masih TERKUNCI. Ini kondisi aman sebelum keputusan Owner go-live.';}else if(liveArmed){title='PRODUCTION ARMED';intro='Kedua kunci production aktif. Core tetap memeriksa identity database, Cabang JLX Cargo, UAT proof, COA, snapshot, approval dan payload pada setiap write.';}else if(partialArm){title='PERINGATAN — hanya satu kunci aktif';intro='Write tetap diblokir oleh core. Samakan kembali kedua kunci sesuai tahap operasional.';}
 return new Response(`<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Accurate Production Readiness | Libra</title><style>*{box-sizing:border-box}body{margin:0;background:#f2f6fa;color:#10243d;font-family:Inter,system-ui}.top{background:#061d36;color:#fff;padding:18px}.topin,.wrap{max-width:1050px;margin:auto}.topin{display:flex;justify-content:space-between;gap:12px}.top a{color:#fff}.wrap{padding:22px 16px 50px}.hero,.card{border-radius:18px;padding:20px}.hero{background:${readyToArm?'#e7f6ed':liveArmed?'#e8f1fb':partialArm?'#fff0ef':'#fff6df'};border:1px solid ${readyToArm?'#b9dfc8':liveArmed?'#b9cee4':partialArm?'#efb9b4':'#eed79a'}}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.stat,.card{background:#fff;border:1px solid #dce6ef}.stat{padding:14px;border-radius:14px}.stat small,.stat b{display:block}.card{margin-top:14px}.ok{color:#176b37}.bad{color:#a12e27}.warn{color:#8b6511}.tablewrap{overflow:auto}table{width:100%;min-width:650px;border-collapse:collapse;font-size:12px}th,td{padding:9px;border-bottom:1px solid #edf1f5;text-align:left}th{background:#f7fafc}code{word-break:break-all}.next{background:#eef5fb;border-left:5px solid #0b4d7a}@media(max-width:700px){.grid{grid-template-columns:1fr}}</style></head><body><header class="top"><div class="topin"><strong>LIBRA • Accurate Production Readiness</strong><a href="/admin-accurate">← Accurate Bridge</a></div></header><main class="wrap"><section class="hero"><h1>${esc(title)}</h1><p>${esc(intro)}</p></section>${connectionError?`<section class="card"><b class="bad">Connection error:</b> ${esc(connectionError)}</section>`:''}<section class="grid"><div class="stat"><small>Live UAT Rp10.000 — JLX Cargo</small>${badge(uat.passed,'PASSED','BELUM')}<small>${uat.job?`${esc(uat.job.journalNumber)} • ${esc(uat.job.postedAt||'')}`:'Butuh UAT baru dengan target cabang'}</small></div><div class="stat"><small>Cabang Target</small>${badge(branch.ok,'FOUND','MISSING')}<small>${esc(branch.target||'JLX Cargo')}${branch.found?` • ID ${esc(branch.found.id)}`:''}</small></div><div class="stat"><small>Kunci 1 — POSTING_ENABLED</small>${badge(key1Locked,'TERKUNCI','AKTIF')}<small>Env ACCURATE_POSTING_ENABLED</small></div><div class="stat"><small>Kunci 2 — PRODUCTION_ARMED</small>${badge(key2Locked,'TERKUNCI','AKTIF')}<small>Env ACCURATE_PRODUCTION_ARMED</small></div><div class="stat"><small>COA Live</small>${badge(validation.ok,'VALID','BELUM VALID')}<small>${(validation.mapped||[]).filter(x=>x.found).length}/${(validation.mapped||[]).length} mapping ditemukan</small></div><div class="stat"><small>Database Terhubung</small><b class="${currentIsTest?'warn':currentName?'ok':'bad'}">${esc(currentName||'TIDAK TERDETEKSI')}</b><small>${currentIsTest?'Masih database TEST':'Bukan nama database test'}</small></div><div class="stat"><small>Identity Lock</small>${badge(productionDbVerified,'MATCH','BELUM MATCH')}<small>Expected: ${esc(expected||'BELUM DISET')}</small></div></section><section class="card"><h2>COA Production Check</h2><div class="tablewrap"><table><thead><tr><th>Role</th><th>No Akun</th><th>Status</th><th>Nama Akun</th></tr></thead><tbody>${mappingRows||'<tr><td colspan="4">Belum dapat membaca COA.</td></tr>'}</tbody></table></div></section><section class="card next"><h2>Urutan aman pindah ke Production</h2><p><b>1.</b> Pastikan master Cabang <b>JLX Cargo</b> FOUND. <b>2.</b> Jalankan UAT Rp10.000 baru dengan branchName JLX Cargo sampai POSTED. <b>3.</b> Import COA final ke database Accurate actual. <b>4.</b> Install Libra Super Admin dan token actual. <b>5.</b> Set <code>ACCURATE_PRODUCTION_DATABASE_NAME</code> persis nama database actual. <b>6.</b> Pastikan UAT cabang PASSED, Cabang FOUND, COA VALID, Identity MATCH, dan kedua kunci TERKUNCI. <b>7.</b> Baru atas keputusan Owner aktifkan dua kunci production.</p><p><b>Fail-safe:</b> semua Journal Voucher dari Libra memakai target <code>branchName=${esc(branch.target||'JLX Cargo')}</code>. Jika cabang tidak ditemukan, production POST ditolak.</p><p><b>Halaman ini tidak melakukan write ke Accurate.</b></p></section></main></body></html>`,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'"}});
};
export const config={path:'/admin-accurate/readiness'};
