import { accurateConfigStatus, accurateJobJson, getAccurateJob, listAccurateJobs, testAccurateConnection, validateAccurateAccountMap } from './_accurate-core.mjs';
import { writeAdminAudit } from './_admin-audit-core.mjs';
import { assertAdminPermission } from './_admin-rbac-core.mjs';
import { getAdminSession } from './_partner-core.mjs';

const esc=v=>String(v??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
const rp=v=>`Rp${Math.trunc(Number(v)||0).toLocaleString('id-ID')}`;
function sameOrigin(request){const origin=request.headers.get('origin');if(!origin)return true;try{return new URL(origin).origin===new URL(request.url).origin;}catch{return false;}}
function stamp(v){try{return new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Jayapura'}).format(new Date(v))+' WIT';}catch{return v||'—';}}

export default async request=>{
 const session=getAdminSession(request);if(!session)return Response.redirect(new URL('/libra-admin-login.html',request.url),302);try{assertAdminPermission(session,'finance.reconcile');}catch{return new Response('Forbidden',{status:403});}
 const url=new URL(request.url);const downloadJob=String(url.searchParams.get('jobId')||'');if(request.method==='GET'&&url.searchParams.get('format')==='json'&&downloadJob){const job=await getAccurateJob(downloadJob);if(!job)return new Response('Job not found',{status:404});return new Response(accurateJobJson(job),{headers:{'content-type':'application/json; charset=utf-8','content-disposition':`attachment; filename="accurate-${job.jobId}.json"`,'cache-control':'no-store'}});}
 let message='',error='',test=null,validation=null;if(request.method==='POST'){
  if(!sameOrigin(request))return new Response('Forbidden',{status:403});const form=await request.formData();const action=String(form.get('action')||'');try{
   if(action==='test_connection'){test=await testAccurateConnection();await writeAdminAudit({session,request,action:'ACCURATE_CONNECTION_TEST',entityType:'ACCURATE',entityId:'CONNECTION',after:{mode:test.mode,host:test.host,database:test.database,testedAt:test.testedAt},note:'Tes koneksi Accurate berhasil; credential tidak dicatat.'});message='Koneksi Accurate berhasil diverifikasi.';}
   else if(action==='validate_accounts'){validation=await validateAccurateAccountMap();await writeAdminAudit({session,request,action:'ACCURATE_ACCOUNT_MAP_VALIDATE',entityType:'ACCURATE',entityId:'ACCOUNT_MAP',after:validation,note:'Validasi mapping Chart of Accounts Accurate.'});message=validation.ok?'Semua account mapping yang diisi ditemukan di Accurate.':'Ada account mapping yang belum ditemukan.';}
   else throw new Error('Aksi tidak dikenal.');
  }catch(e){error=e?.message||'Accurate bridge gagal.';}
 }else if(request.method!=='GET')return new Response('Method not allowed',{status:405});
 const config=accurateConfigStatus();const jobs=await listAccurateJobs(120);const mapping=Object.entries(config.accountMap).map(([role,no])=>`<tr><td><b>${esc(role)}</b></td><td><code>${esc(no||'BELUM DISET')}</code></td></tr>`).join('');const rows=jobs.map(j=>`<tr><td><b>${esc(j.jobId)}</b><small>${esc(stamp(j.createdAt))}</small></td><td>${esc(j.partnerId)}<small>${esc(j.month)}</small></td><td>${esc(j.statementNo)}</td><td><span class="pill ${j.status==='READY_FOR_REVIEW'?'ok':''}">${esc(j.status)}</span></td><td>${rp(j.journalDraft?.totalDebit)} / ${rp(j.journalDraft?.totalCredit)}<small>${j.journalDraft?.balanced?'Balanced':'Tidak balanced'}</small></td><td><a href="?jobId=${encodeURIComponent(j.jobId)}&format=json">Export JSON</a></td></tr>`).join('');
 const validationRows=validation?.mapped?.map(x=>`<li>${esc(x.role)} • ${esc(x.no)} • ${x.found?'✓ '+esc(x.name):'✗ tidak ditemukan'}</li>`).join('')||'';
 return new Response(`<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Accurate Bridge | Libra</title><style>*{box-sizing:border-box}body{margin:0;background:#f2f6fa;color:#10243d;font-family:Inter,system-ui}.top{background:#061d36;color:#fff;padding:18px}.topin,.wrap{max-width:1200px;margin:auto}.topin{display:flex;justify-content:space-between}.top a{color:#fff}.wrap{padding:24px 18px 50px}.hero{background:linear-gradient(135deg,#0b2d52,#155a7e);color:#fff;border-radius:20px;padding:24px}.notice{padding:12px;border-radius:10px;margin:12px 0}.okn{background:#e8f6ee;color:#176b37}.err{background:#fff0ef;color:#9e2621}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:15px}.stat,.card{background:#fff;border:1px solid #dce6ef;border-radius:15px}.stat{padding:14px}.stat small{display:block;color:#718397}.stat b{display:block;font-size:18px;margin-top:4px}.card{padding:16px;margin-top:15px}.actions{display:flex;gap:8px;flex-wrap:wrap}button{font:inherit;border:0;border-radius:8px;background:#0b2d52;color:#fff;padding:9px 12px;font-weight:800}.tablewrap{overflow:auto}table{width:100%;min-width:760px;border-collapse:collapse;font-size:12px}th,td{padding:9px;border-bottom:1px solid #edf1f5;text-align:left;vertical-align:top}th{background:#f7fafc}small{display:block;color:#718397;margin-top:4px}.pill{display:inline-block;padding:5px 8px;border-radius:999px;background:#fff1d9;font-weight:800}.pill.ok{background:#dff3e7;color:#176b37}code{word-break:break-all}.lock{border-left:5px solid #d49b2d;background:#fff9ea}.nav{display:flex;gap:14px;flex-wrap:wrap;margin-top:16px}.nav a{font-weight:800;color:#0b426e}@media(max-width:760px){.grid{grid-template-columns:1fr}}</style></head><body><header class="top"><div class="topin"><strong>LIBRA • Accurate Online Bridge</strong><a href="/libra-admin">← Home Admin</a></div></header><main class="wrap"><section class="hero"><h1>Accurate Online Bridge</h1><p>Server-to-server connection check, Chart of Accounts validation dan queue jurnal dari statement Libra. Secret/token tidak pernah tampil di halaman atau audit.</p></section>${message?`<div class="notice okn">${esc(message)}</div>`:''}${error?`<div class="notice err">${esc(error)}</div>`:''}<section class="grid"><div class="stat"><small>Auth Mode</small><b>${esc(config.authMode)}</b></div><div class="stat"><small>Account Mapping</small><b>${config.mappingReady?'READY':'BELUM LENGKAP'}</b></div><div class="stat"><small>Queue</small><b>${jobs.length}</b></div></section><section class="card lock"><h2>Posting Guard</h2><p>${esc(config.postingNote)}</p><p>Artinya sistem sudah boleh menyiapkan jurnal dan memeriksa akun, tetapi belum dapat mengirim jurnal live sebelum Finance menyetujui mapping final dan schema transaksi Accurate yang digunakan.</p></section><section class="card"><div class="actions"><form method="post"><input type="hidden" name="action" value="test_connection"><button>Test Koneksi Accurate</button></form><form method="post"><input type="hidden" name="action" value="validate_accounts"><button>Validasi Chart of Accounts</button></form></div>${test?`<p><b>Host:</b> ${esc(test.host)}<br><b>Mode:</b> ${esc(test.mode)}<br><b>Account probe:</b> ${esc(test.accountProbe?.name||test.accountProbe?.error||'OK')}</p>`:''}${validationRows?`<ul>${validationRows}</ul>`:''}</section><section class="card"><h2>Account Mapping dari Environment</h2><div class="tablewrap"><table><thead><tr><th>Role Akun</th><th>No Akun Accurate</th></tr></thead><tbody>${mapping}</tbody></table></div><p><small>Mapping minimum: Customer Deposit, Service Revenue, Claim Expense, Adjustment Expense. Nomor akun disimpan sebagai environment variable, bukan di repository.</small></p></section><section class="card"><h2>Accurate Sync Queue</h2><div class="tablewrap"><table><thead><tr><th>Job</th><th>Partner/Periode</th><th>Statement</th><th>Status</th><th>Debit/Credit</th><th>Export</th></tr></thead><tbody>${rows||'<tr><td colspan="6">Belum ada queue.</td></tr>'}</tbody></table></div></section><div class="nav"><a href="/admin-finance-billing">Finance & Billing →</a><a href="/admin-approvals">Approval Center →</a></div></main></body></html>`,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'none'"}});
};
export const config={path:'/admin-accurate'};
