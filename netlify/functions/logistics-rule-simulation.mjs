import { getAdminSession } from './_partner-core.mjs';
import { allocateOperationalCosts } from './_smu-cost-allocation-core.mjs';
import { HUB_COST_RULES, LASTMILE_COST_RULES, LOGISTICS_RULE_VERSION } from './_logistics-rule-core.mjs';

const esc=v=>String(v??'').replace(/[&<>\'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
const rp=v=>`Rp${Math.round(Number(v)||0).toLocaleString('id-ID')}`;

const SCENARIO=[
 {bookingId:'SIM-CGK-DG-A',hubCode:'CGK',chargeableWeightKg:100,isDgGadget:true,isFreshFrozen:false,isLastmileIncoming:false,smus:[{smuNumber:'SIM-SMU-001',weightKg:100}]},
 {bookingId:'SIM-CGK-DG-B',hubCode:'CGK',chargeableWeightKg:300,isDgGadget:true,isFreshFrozen:false,isLastmileIncoming:false,smus:[{smuNumber:'SIM-SMU-001',weightKg:300}]},
 {bookingId:'SIM-CGK-FRESH-C',hubCode:'CGK',chargeableWeightKg:100,isDgGadget:false,isFreshFrozen:true,isLastmileIncoming:false,smus:[{smuNumber:'SIM-SMU-001',weightKg:100}]},
 {bookingId:'SIM-LM-A',hubCode:'DJJ',chargeableWeightKg:40,isDgGadget:false,isFreshFrozen:false,isLastmileIncoming:true,smus:[{smuNumber:'SIM-SMU-LM-001',weightKg:40}]},
 {bookingId:'SIM-LM-B',hubCode:'DJJ',chargeableWeightKg:60,isDgGadget:false,isFreshFrozen:false,isLastmileIncoming:true,smus:[{smuNumber:'SIM-SMU-LM-001',weightKg:60}]},
];

export default async request=>{
 const session=getAdminSession(request);if(!session)return Response.redirect(new URL('/libra-admin-login.html',request.url),302);if(!['OPS','FINANCE','SUPERADMIN'].includes(String(session.role||'').toUpperCase()))return new Response('Akses ditolak.',{status:403});if(request.method!=='GET')return new Response('Method not allowed',{status:405});
 const result=allocateOperationalCosts(SCENARIO);
 const rows=result.bookings.map(b=>`<tr><td><b>${esc(b.bookingId)}</b></td><td>${rp(b.totalCost)}</td><td>${b.components.map(c=>`<div><b>${esc(c.code)}</b> — ${rp(c.amount)} <small>${esc(c.basis)}${c.smuNumber?` • ${esc(c.smuNumber)}`:''}</small></div>`).join('')}</td></tr>`).join('');
 const dgTotal=result.bookings.flatMap(b=>b.components).filter(c=>c.code==='CGK_DG_GADGET_AVSEC').reduce((s,c)=>s+c.amount,0);
 const smuHandlingTotal=result.bookings.flatMap(b=>b.components).filter(c=>c.code==='CGK_SMU_HANDLING').reduce((s,c)=>s+c.amount,0);
 const lmHandlingTotal=result.bookings.flatMap(b=>b.components).filter(c=>c.code==='LASTMILE_HANDLING_BARANG').reduce((s,c)=>s+c.amount,0);
 const checks=[
  ['CGK Handling SMU hanya sekali untuk SIM-SMU-001',smuHandlingTotal===HUB_COST_RULES.CGK.smuHandlingPerSmu,smuHandlingTotal,HUB_COST_RULES.CGK.smuHandlingPerSmu],
  ['DG Gadget hanya Rp200.000 sekali per SMU dan hanya dibagi ke booking DG',dgTotal===HUB_COST_RULES.CGK.dgGadgetPerSmu,dgTotal,HUB_COST_RULES.CGK.dgGadgetPerSmu],
  ['Last-mile handling barang hanya Rp25.000 sekali untuk SMU gabungan',lmHandlingTotal===LASTMILE_COST_RULES.handlingBarangPerSmu,lmHandlingTotal,LASTMILE_COST_RULES.handlingBarangPerSmu],
 ];
 const checkHtml=checks.map(([label,ok,actual,expected])=>`<div class="check ${ok?'good':'bad'}"><b>${ok?'✓ PASS':'✕ FAIL'}</b><span>${esc(label)}</span><small>Actual ${rp(actual)} • Expected ${rp(expected)}</small></div>`).join('');
 const html=`<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Simulasi Rule Logistik | Libra</title><style>*{box-sizing:border-box}body{margin:0;background:#f3f6f9;color:#10243d;font-family:Inter,system-ui}.top{background:#061d36;color:#fff;padding:18px}.topin,.wrap{max-width:1100px;margin:auto}.topin{display:flex;justify-content:space-between}.top a{color:#fff}.wrap{padding:22px 14px 50px}.panel{background:#fff;border:1px solid #dbe5ee;border-radius:18px;padding:18px;margin-bottom:15px}.note{background:#eef5fb;border-left:5px solid #0b5c9c}.checks{display:grid;gap:10px}.check{padding:13px;border-radius:12px;display:grid;gap:4px}.good{background:#e9f7ee}.bad{background:#fff0ef}.check small,td small{color:#6c7f91}.tablewrap{overflow:auto}table{width:100%;border-collapse:collapse;min-width:780px}th,td{padding:11px;border-bottom:1px solid #e5ebf0;text-align:left;vertical-align:top}th{background:#f7fafc}td div{padding:4px 0}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.card{padding:12px;background:#f7fafc;border-radius:12px}.card small,.card b{display:block}.card b{font-size:18px;margin-top:3px}@media(max-width:720px){.cards{grid-template-columns:1fr}.topin{gap:10px}}</style></head><body><header class="top"><div class="topin"><strong>LIBRA • Logistics Rule UAT</strong><a href="/admin-tool">← Home Admin</a></div></header><main class="wrap"><h1>Simulasi Cost Allocation</h1><p>Read-only. Halaman ini <b>tidak menulis booking, wallet, vendor cost, invoice atau Accurate</b>.</p><section class="panel note"><b>Rule version ${esc(LOGISTICS_RULE_VERSION)}</b><p>Biaya per-SMU wajib dikenakan sekali pada SMU/consolidation lalu dialokasikan. Airline freight dan airline surcharge tidak dihitung di sini karena tetap berasal dari master airline/vendor.</p></section><section class="panel"><div class="cards"><div class="card"><small>CGK RA</small><b>${rp(HUB_COST_RULES.CGK.raPerKg)}/kg</b></div><div class="card"><small>CGK Gudang</small><b>${rp(HUB_COST_RULES.CGK.warehouseHandlingPerKg)}/kg</b></div><div class="card"><small>CGK Handling SMU</small><b>${rp(HUB_COST_RULES.CGK.smuHandlingPerSmu)}/SMU</b></div><div class="card"><small>DG Gadget</small><b>${rp(HUB_COST_RULES.CGK.dgGadgetPerSmu)}/SMU</b></div><div class="card"><small>Karantina Fresh/Frozen</small><b>${rp(HUB_COST_RULES.CGK.quarantineFreshFrozenPerKg)}/kg</b></div><div class="card"><small>Last-mile Handling Barang</small><b>${rp(LASTMILE_COST_RULES.handlingBarangPerSmu)}/SMU</b></div></div></section><section class="panel"><h2>Safety Checks</h2><div class="checks">${checkHtml}</div></section><section class="panel"><h2>Alokasi per Booking</h2><div class="tablewrap"><table><thead><tr><th>Booking</th><th>Total Operational Cost</th><th>Komponen</th></tr></thead><tbody>${rows}</tbody></table></div><p><b>Total simulasi:</b> ${rp(result.totalCost)} • ${result.bookingCount} booking • ${result.smuCount} SMU unik.</p></section></main></body></html>`;
 return new Response(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'"}});
};
export const config={path:'/admin-logistics-rules/simulation'};
