import { getMasterSnapshot } from './_master-sheet-core.mjs';
import { verifyRateAccess } from './_partner-rate-access-core.mjs';

const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const money=v=>Number.isFinite(Number(v))?`Rp${Math.round(Number(v)).toLocaleString('id-ID')}`:'Konfirmasi';
const kg=v=>Number.isFinite(Number(v))&&Number(v)>0?`${Number(v).toLocaleString('id-ID')} kg`:'—';
function stamp(v){if(!v)return '—';try{return new Intl.DateTimeFormat('id-ID',{dateStyle:'long',timeStyle:'short',timeZone:'Asia/Jayapura'}).format(new Date(v))+' WIT';}catch{return String(v);}}
function cookie(request,name){const raw=request.headers.get('cookie')||'';for(const part of raw.split(';')){const [k,...rest]=part.trim().split('=');if(k===name)return rest.join('=');}return '';}
function shell(content,title='Daftar Harga Partner'){return `<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="Daftar harga partner PT Libra Jaya Logistic untuk perusahaan logistik terverifikasi."><title>${esc(title)} | Libra Jaya Logistic</title><style>*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,-apple-system,sans-serif;background:#f3f6f9;color:#10243d}.top{background:#061d36;color:#fff}.topin,.wrap{max-width:1180px;margin:auto}.topin{display:flex;justify-content:space-between;align-items:center;padding:16px}.top a{color:#fff;text-decoration:none}.wrap{padding:24px 14px 60px}.hero{background:linear-gradient(135deg,#0b2d52,#0b5f8e);color:#fff;border-radius:22px;padding:26px}.hero h1{font-size:34px;margin:6px 0}.hero p{max-width:840px;line-height:1.55;color:#d9e9f3}.eyebrow{font-size:11px;font-weight:900;letter-spacing:.12em}.card{margin-top:16px;background:#fff;border:1px solid #dbe5ee;border-radius:18px;padding:20px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field label{display:block;font-weight:800;font-size:12px;margin-bottom:6px}.field input{width:100%;padding:12px;border:1px solid #cbd8e4;border-radius:10px;font:inherit}.consent{display:flex;gap:9px;align-items:flex-start;margin:14px 0;font-size:12px;color:#5d7184}.consent input{margin-top:3px}.submit{width:100%;border:0;border-radius:11px;padding:13px;background:#0b426e;color:#fff;font:inherit;font-weight:900}.note{margin-top:12px;background:#eef6ff;border:1px solid #c9dff3;border-radius:12px;padding:12px;font-size:12px;line-height:1.55;color:#3f627c}.verify{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.verify span{padding:7px 10px;border-radius:999px;background:#e7f6ed;color:#176b37;font-size:11px;font-weight:900}.search{display:flex;gap:8px;margin:16px 0}.search input{flex:1;padding:12px;border:1px solid #cbd8e4;border-radius:10px;font:inherit}.search button{padding:12px 16px;border:0;border-radius:10px;background:#0b426e;color:#fff;font-weight:850}.tablebox{background:#fff;border:1px solid #dbe5ee;border-radius:16px;overflow:auto}table{border-collapse:collapse;width:100%;min-width:760px}th,td{padding:12px;border-bottom:1px solid #edf1f5;text-align:left;font-size:13px;vertical-align:top}th{background:#f7fafc;color:#5e7285}td small{display:block;margin-top:3px;color:#708397}.mobile{display:none}.rules{margin-top:20px;display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.rule{background:#fff;border:1px solid #dbe5ee;border-radius:16px;padding:17px}.rule h2{font-size:18px;margin:0 0 10px}.rule ul{padding-left:18px;margin:0;color:#526b80;line-height:1.65;font-size:13px}.rule b{color:#10243d}.rate h3{margin:0}.rate p{margin:3px 0 10px;color:#718397;font-size:12px}.rate>strong{display:block;font-size:22px;color:#0b5f8e;margin-bottom:10px}.rate>strong small{font-size:11px}.facts{display:grid;grid-template-columns:1fr 1fr;gap:6px}.facts span{background:#f5f8fa;padding:8px;border-radius:8px;font-size:10px;color:#718397}.facts b{display:block;margin-top:2px;color:#243b52;font-size:11px}.footer-note{margin-top:18px;background:#fff8df;border:1px solid #ead690;border-radius:13px;padding:13px;font-size:12px;line-height:1.55}@media(max-width:700px){.hero h1{font-size:27px}.grid,.rules{grid-template-columns:1fr}.tablebox{display:none}.mobile{display:grid;gap:9px}.rate{background:#fff;border:1px solid #dbe5ee;border-radius:15px;padding:14px}.topin{align-items:flex-start}.search{position:sticky;top:0;background:#f3f6f9;padding:8px 0;z-index:2}}</style></head><body><header class="top"><div class="topin"><strong>PT LIBRA JAYA LOGISTIC</strong><a href="/">← Website Utama</a></div></header><main class="wrap">${content}</main></body></html>`;}

function gate(){return shell(`<section class="hero"><span class="eyebrow">AKSES KHUSUS PERUSAHAAN LOGISTIK</span><h1>Daftar Harga Partner</h1><p>Daftar harga hanya ditampilkan kepada perusahaan logistik yang dapat diverifikasi. Isi data perusahaan di bawah; AI Libra akan memeriksa NIB melalui sumber web resmi. Jika bukti belum cukup, permintaan masuk review admin dan tidak ditolak otomatis.</p></section><section class="card"><h2>Verifikasi Perusahaan</h2><div class="note"><b>Privasi:</b> AI hanya menerima <b>Nama Perusahaan + NIB</b> untuk verifikasi. Nama PIC dan nomor HP tidak dikirim ke model; data tersebut hanya digunakan Libra untuk tindak lanjut partner.</div><form method="post" action="/.netlify/functions/public-partner-rate-verify"><div class="grid"><div class="field"><label>Nama Perusahaan</label><input name="companyName" autocomplete="organization" placeholder="PT Contoh Logistik Indonesia" maxlength="180" required></div><div class="field"><label>NIB 13 Digit</label><input name="nib" inputmode="numeric" pattern="[0-9]{13}" minlength="13" maxlength="13" placeholder="1234567890123" required></div><div class="field"><label>Nama PIC</label><input name="picName" autocomplete="name" maxlength="120" placeholder="Nama penanggung jawab" required></div><div class="field"><label>No. HP PIC</label><input name="phone" inputmode="tel" autocomplete="tel" maxlength="16" placeholder="08xxxxxxxxxx" required></div></div><label class="consent"><input type="checkbox" name="consent" required><span>Saya menyetujui PT Libra Jaya Logistic memeriksa NIB/nama perusahaan pada sumber publik untuk memastikan akses daftar harga diberikan khusus kepada perusahaan logistik.</span></label><button class="submit" type="submit">Verifikasi & Lihat Harga</button></form></section>`,`Verifikasi Daftar Harga Partner`);}

export default async request=>{
 if(request.method!=='GET')return new Response('Method not allowed',{status:405});
 const access=verifyRateAccess(cookie(request,'libra_rate_access'));if(!access)return new Response(gate(),{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','referrer-policy':'no-referrer','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'"}});
 const snapshot=await getMasterSnapshot().catch(()=>null);if(!snapshot)return new Response('Daftar harga sedang diperbarui.',{status:503});
 const url=new URL(request.url),q=String(url.searchParams.get('q')||'').trim().toLowerCase();
 const all=(snapshot.routes||[]).filter(r=>r.coverageStatus==='ACTIVE'&&Number(r.tarifRekomKg)>0),routes=all.filter(r=>!q||[r.kelurahan,r.distrik,r.kabupatenKota,r.hub,r.kodeRute].some(v=>String(v||'').toLowerCase().includes(q)));
 const rows=routes.map(r=>`<tr><td><b>${esc(r.kelurahan||'—')}</b><small>${esc(r.distrik||'')} • ${esc(r.kabupatenKota||'')}</small></td><td>${esc(r.hub||r.bandaraAsal||'DJJ')}</td><td><b>${money(r.tarifRekomKg)}/kg</b><small>Min. charge ${kg(r.minimumChargeableKg||10)}</small></td><td>${esc(r.slaTotalHub||r.slaLastmile||r.slaMaster||'Konfirmasi')}</td><td>${Number(r.minimumLoadKg)>0?`Konsolidasi min. ${kg(r.minimumLoadKg)}`:'Reguler'}</td></tr>`).join('');
 const cards=routes.map(r=>`<article class="rate"><h3>${esc(r.kelurahan||'—')}</h3><p>${esc(r.distrik||'')} • ${esc(r.kabupatenKota||'')}</p><strong>${money(r.tarifRekomKg)}<small>/kg</small></strong><div class="facts"><span>Hub<b>${esc(r.hub||r.bandaraAsal||'DJJ')}</b></span><span>Min. charge<b>${kg(r.minimumChargeableKg||10)}</b></span><span>SLA<b>${esc(r.slaTotalHub||r.slaLastmile||r.slaMaster||'Konfirmasi')}</b></span><span>Skema<b>${Number(r.minimumLoadKg)>0?`Konsolidasi ${kg(r.minimumLoadKg)}`:'Reguler'}</b></span></div></article>`).join('');
 const content=`<section class="hero"><span class="eyebrow">PARTNER TERVERIFIKASI</span><h1>Daftar Harga Partner Terbaru</h1><p>Akses untuk <b>${esc(access.companyName)}</b> • NIB ${esc(access.nib)}. Harga ditarik dari Master operasional terbaru dan dapat berubah ketika master harga dipublikasikan ulang.</p><div class="verify"><span>✓ NIB TERVERIFIKASI</span><span>✓ PERUSAHAAN LOGISTIK</span><span>Update ${esc(stamp(snapshot.syncedAt||snapshot.publishedAt))}</span></div></section><form class="search" method="get"><input name="q" value="${esc(url.searchParams.get('q')||'')}" placeholder="Cari kelurahan, distrik, kota atau kode rute"><button>Cari</button></form><section class="tablebox"><table><thead><tr><th>Tujuan</th><th>Hub</th><th>Tarif Partner</th><th>SLA</th><th>Skema</th></tr></thead><tbody>${rows||'<tr><td colspan="5">Tidak ada rute aktif yang cocok.</td></tr>'}</tbody></table></section><section class="mobile">${cards||'<div class="card">Tidak ada rute aktif yang cocok.</div>'}</section><section class="rules"><article class="rule"><h2>Rule Booking & Berat</h2><ul><li>Booking hanya untuk rute yang berstatus <b>AKTIF</b>.</li><li>Minimum charge mengikuti tarif rute; master pilot saat ini memakai minimum <b>10 kg</b>.</li><li>Incoming Port-to-Door/last-mile partner menggunakan <b>PTI Partner</b> sebagai basis berat.</li><li>Outgoing JL Express menggunakan berat booking sebagai estimasi; setelah transit/repacking di Soetta, <b>Final Chargeable Weight</b> menjadi basis faktur.</li><li>Selisih outgoing <b>&lt; 0,20 kg = CLEAR</b>; selisih <b>≥ 0,20 kg = WEIGHT_ADJUSTMENT</b>.</li></ul></article><article class="rule"><h2>Rule Operasional</h2><ul><li>Rute minimum load mengikuti konsolidasi total booking pada rute/cut-off yang sama.</li><li>DG gadget/lithium battery, fresh/frozen, karantina, packing kayu, asuransi, pickup dan layanan khusus dapat memiliki surcharge sesuai layanan.</li><li>Barang wajib layak angkut; repacking dapat mengubah berat/volume final.</li><li>SLA dapat terdampak cuaca, flight disruption, force majeure, pembatasan akses dan kondisi keselamatan.</li><li>Harga khusus kontrak, volume, atau API partner berlaku setelah persetujuan partner dan tidak ditampilkan di halaman publik ini.</li></ul></article></section><div class="footer-note"><b>Referensi harga terbaru:</b> setiap booking tetap menghasilkan quotation/konfirmasi harga final. Halaman ini tidak menampilkan modal, cost vendor, margin, maupun data internal Libra.</div>`;
 return new Response(shell(content),{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','referrer-policy':'no-referrer','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'"}});
};
export const config={path:'/harga-partner'};
