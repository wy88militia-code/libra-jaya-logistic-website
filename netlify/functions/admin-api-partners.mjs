import { listApiLogs } from './_api-auth.mjs';
import { listPartners, validAdminSession } from './_partner-core.mjs';
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function mask(key){const s=String(key||'');return s.length>10?`${s.slice(0,7)}…${s.slice(-4)}`:'—';}
export default async request=>{
 if(!validAdminSession(request))return Response.redirect(new URL('/admin-login.html',request.url),302);if(request.method!=='GET')return new Response('Method not allowed',{status:405});
 const [partners,logs]=await Promise.all([listPartners(),listApiLogs(300)]);const partnerRows=partners.map(p=>`<tr><td><strong>${esc(p.partnerId)}</strong><small>${esc(p.companyName)}</small></td><td>${esc(p.status)}</td><td><code>${esc(mask(p.apiKey))}</code></td><td>${p.apiSecret?'Aktif':'Belum ada'}</td></tr>`).join('');const logRows=logs.map(l=>`<tr><td>${esc(l.createdAt)}</td><td>${esc(l.partnerId)}</td><td>${esc(l.method)} ${esc(l.path)}</td><td>${esc(l.action||'—')}</td><td>${esc(l.status)}</td><td>${esc(l.reference||'—')}<small>${esc(l.error||'')}</small></td></tr>`).join('');
 return new Response(`<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>API Partner | Libra</title><style>*{box-sizing:border-box}body{margin:0;background:#f2f6fa;color:#10243d;font-family:Inter,system-ui}.top{background:#061d36;color:#fff;padding:18px}.topin{max-width:1200px;margin:auto;display:flex;justify-content:space-between}.top a{color:#fff}.wrap{max-width:1200px;margin:auto;padding:24px 18px}.card{background:#fff;border:1px solid #dce5ee;border-radius:16px;overflow:hidden;margin:18px 0}.head{padding:15px 17px;border-bottom:1px solid #e8eef4}.head h2{margin:0}.tablewrap{overflow:auto}table{width:100%;min-width:760px;border-collapse:collapse;font-size:13px}th,td{padding:10px 12px;border-bottom:1px solid #edf1f5;text-align:left;vertical-align:top}th{background:#f7fafc}small{display:block;color:#718397;margin-top:4px}.spec{background:#071d33;color:#d9e8f5;padding:16px;border-radius:12px;overflow:auto}code{font-family:ui-monospace,monospace}</style></head><body><header class="top"><div class="topin"><strong>LIBRA JAYA LOGISTIC • API Partner</strong><a href="/admin-tool">← Home Admin</a></div></header><main class="wrap"><h1>API Partner & Audit</h1><p>API key mengidentifikasi partner. API secret tidak ditampilkan lagi setelah dibuat.</p><section class="card"><div class="head"><h2>Kredensial Partner</h2></div><div class="tablewrap"><table><thead><tr><th>Partner</th><th>Status</th><th>API Key</th><th>Secret</th></tr></thead><tbody>${partnerRows}</tbody></table></div></section><section class="card"><div class="head"><h2>Signature v1</h2></div><div style="padding:16px"><pre class="spec">METHOD\nPATH_WITH_QUERY\nTIMESTAMP\nNONCE\nSHA256(BODY)</pre><p>Header: <code>x-libra-key</code>, <code>x-libra-timestamp</code>, <code>x-libra-nonce</code>, <code>x-libra-signature</code>. Signature = HMAC-SHA256 base64url memakai API Secret. Toleransi waktu ±5 menit dan nonce hanya boleh digunakan sekali.</p></div></section><section class="card"><div class="head"><h2>Log API</h2></div><div class="tablewrap"><table><thead><tr><th>Waktu</th><th>Partner</th><th>Request</th><th>Aksi</th><th>HTTP</th><th>Referensi/Error</th></tr></thead><tbody>${logRows||'<tr><td colspan="6">Belum ada log API.</td></tr>'}</tbody></table></div></section></main></body></html>`,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'"}});
};
export const config={path:'/admin-api-partners'};
